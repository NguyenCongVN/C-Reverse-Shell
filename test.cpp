#include <iostream>
#include <iomanip>
#include <string.h>

using namespace std;
{
struct student;
charcode[20];
string name;
float attendance midterm final course grade
void input(student s[], int N)
};
    for (int i=0; i<N; i++)
    {
        cout<<"Enter the code of student "<<i+1<<": ";
        cin>>s[i].code;
        cin.ignore();
        cout<<"Name of studet "<< i+1 <<": ";
        getline(cin,s[i].name);
        cout<<"Attendance score "<<i+1<<": ";
        cin>>s[i].n1;
        cout<<"Midterm "<<i+1<<": ";
        cin>>s[i].n2;
        cout<<"Final  "<<i+1<<": ";
        cin>>s[i].final;
        cout<<"Course grade  "<<i+1<<": ";
        cin>>s[i].course grade;
